typedef enum
{
    NO_OK,
    OK
}flagType;

typedef enum
{
    NOT_VALID,
    VALID
}passwordCheckType;

typedef enum
{
    CALC=1,
    DTOB,
    REVERSE,
    SWAP,
    ARRAY,
    EXIT
}selectProgType;

typedef enum
{
    SORT=1,
    AVG,
    LARGEST,
    PRINT,
    RETURN
}arrayType;
